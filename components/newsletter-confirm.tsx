"use client";
import { useState } from 'react';
export function NewsletterConfirm({token,action}:{token:string;action:'confirm'|'unsubscribe'}){const [message,setMessage]=useState('');const [busy,setBusy]=useState(false);return <div className="content-card"><p>{action==='confirm'?'Confirm that you would like to receive new articles by email.':'Remove your email from the subscriber list.'}</p><button className="meadow-button mt-6" disabled={busy||!token} onClick={async()=>{setBusy(true);try{const r=await fetch('/api/newsletter/confirm',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({token,action})});const b=await r.json();setMessage(b.message||b.error);}catch{setMessage('Please try again later.');}finally{setBusy(false);}}}>{busy?'Working…':action==='confirm'?'Confirm subscription':'Unsubscribe'}</button><p className="mt-4" role="status">{message}</p></div>;}
