"use client";
import { useState } from 'react';
export function Newsletter(){
 const [message,setMessage]=useState('');const [busy,setBusy]=useState(false);
 return <section className="content-card"><h2 className="text-2xl font-semibold text-white">Field notes, when there’s something worth sharing.</h2><p className="mt-3">New articles on AI systems, research and building software. Confirm your email to subscribe, or follow the <a className="underline" href="/rss.xml">RSS feed</a>.</p><form className="mt-6 flex flex-wrap gap-3" onSubmit={async e=>{e.preventDefault();setBusy(true);const data=new FormData(e.currentTarget);try{const response=await fetch('/api/newsletter',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email:data.get('email'),website:data.get('website')})});const json=await response.json();setMessage(response.ok?json.message:json.error);}catch{setMessage('Could not connect. Please try again.');}finally{setBusy(false);}}}><label className="sr-only" htmlFor="newsletter-email">Email address</label><input className="min-w-0 flex-1 rounded-xl border border-white/20 bg-[#080c22] px-4 py-3 text-white" id="newsletter-email" name="email" type="email" required maxLength={254} placeholder="you@example.com"/><input className="hidden" name="website" tabIndex={-1} aria-hidden="true" autoComplete="off"/><button className="meadow-button" disabled={busy}>{busy?'Sending…':'Subscribe →'}</button></form><p role="status" className="mt-3 text-sm">{message}</p></section>;
}
