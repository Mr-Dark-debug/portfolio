export const services = {
 'saas-mvp': {
  title: 'SaaS MVP development', eyebrow: 'From a product question to a usable release',
  summary: 'A focused build for founders and small teams who need to test a useful workflow with real users. Python and FastAPI backends, practical AI integrations and a Next.js interface, with scope and handover agreed before implementation.',
  sections: [
   ['Start with the decision the product needs to support', 'An MVP is useful when it helps you learn something concrete. That might be whether a customer can complete a task without assistance, whether a retrieval pipeline finds the right evidence, or whether a team will adopt a new internal workflow. The first step is to name that question and decide what observable behavior would answer it. A feature list alone does not establish what the product needs to prove. We define the main user, the task they are trying to finish and the current obstacle. That becomes the basis for the first release and the acceptance criteria.'],
   ['Make a small, complete workflow', 'A narrow workflow can still be complete. A document assistant, for example, needs more than a chat box: it needs an understandable upload state, a clear answer about supported files, an indication of processing progress, useful retrieval and a way to inspect sources. When something fails, the person needs to know what happened and what they can do next. I plan the first release around this complete path. Secondary features can wait until there is evidence that they support the main task. This keeps the initial build inspectable and makes feedback easier to interpret.'],
   ['Choose the simplest architecture that supports the requirements', 'My strongest backend stack is Python and FastAPI, with PostgreSQL for application data and explicit interfaces for model providers. Next.js can provide the public site and application interface. A background job system is appropriate when ingestion or model work outlives a normal request. These are starting points rather than automatic requirements. An existing product may already have a suitable stack, and a small internal tool may need fewer moving parts. The architecture discussion covers data ownership, hosting constraints, authentication, expected workload and how the team wants to maintain the product after handover.'],
   ['Treat AI as a system that can fail', 'Model output needs a place in the product’s error model. The interface should distinguish a provider timeout from a missing document, an unsupported request and an answer without enough evidence. For structured workflows, I use validation around inputs and model output, with bounded retries and explicit stopping conditions. For generated documents, reviewable changes help the user decide what to keep. The objective is to make the workflow useful even when generation is imperfect. The initial scope should also identify which actions always require a person’s review before they affect customers or external systems.'],
   ['Build retrieval around representative questions', 'If the product needs answers from your documents, retrieval quality depends on the content and the questions people actually ask. Before committing to a particular chunking or embedding approach, we need a small evaluation set with representative questions and expected evidence. That gives us a way to inspect omissions, irrelevant passages and misleading answers. Source links and document boundaries should survive ingestion so that an answer remains traceable. Retrieval is then something we can improve deliberately, rather than a hidden implementation detail whose quality is judged from one impressive demonstration.'],
   ['Keep data handling explicit', 'A product that accepts customer documents or API keys needs clear boundaries. We identify which data belongs in the database, which files belong in object storage and which information should never appear in logs. Access checks belong at the data boundary as well as in the interface. Development and production should use separate configuration, and secrets should be supplied through the hosting environment. Data deletion and export requirements should be discussed early because they affect storage choices. I document the decisions that the product owner needs to understand rather than leaving them implicit in code.'],
   ['Use milestones that can be reviewed', 'I prefer milestones that produce working behavior: a validated input flow, a persistent result, a retrieval evaluation or a deployment that a teammate can open. Each milestone should have a short description of what changed and how it was checked. Screenshots are useful for layout review, but they do not replace exercising the workflow. A clear milestone also makes it easier to adjust scope when research reveals a constraint. We can decide together whether the next increment is worth building instead of discovering at the end that the original assumptions no longer fit.'],
   ['Test the parts where a mistake matters', 'The test plan follows the product’s failure modes. Authentication and authorization need negative tests, not just a successful login. A payment or external action needs idempotency and retry behavior where applicable. Retrieval needs examples that can expose the wrong source being selected. The main browser path should be tested at the sizes and devices the intended users rely on. I also separate build success, local behavior and hosted behavior in delivery notes. A passing build does not prove that a production credential, callback URL or background worker has been configured correctly.'],
   ['Understand deployment before calling the release complete', 'Deployment includes the application, its database schema and the services needed for the main workflow. Environment variables must be checked without exposing their values, and the deployed revision should be identifiable. Where the hosting platform is stateless, files need durable storage. Long-running work needs a suitable execution environment instead of depending on a browser tab staying open. A useful handover describes where the application runs, how to inspect errors and which steps are required to deploy a future change. Provider accounts remain under the owner’s control.'],
   ['Leave the team with a maintainable starting point', 'The final deliverable should include the source repository, setup instructions, environment variable names, migration commands and a description of any known limitations. I aim for a codebase where a developer can follow a request through validation, business logic and storage without guessing which layer owns the behavior. If there are practical trade-offs in the first release, those should be written down with their consequences. That might include a limited input format, a manual operational step or an integration that still needs a vendor account. Clear limitations make future planning more reliable.'],
   ['What to bring to the first conversation', 'A short description of the user problem is enough to start. Examples of the current workflow, a few representative inputs and the constraints you already know are especially helpful. If there is an existing codebase, say who maintains it and where it is hosted. Mention deadlines, budget boundaries, required integrations and whether real customer data is involved. You do not need a finished specification. The goal of the conversation is to decide what information is missing and whether a small implementation can answer the product question within a sensible scope.'],
   ['How scope and availability are agreed', 'I am based in Trier, Germany, and currently pursuing a master’s degree in Natural Language Processing. Project work needs to fit the availability and scope we explicitly agree. I do not publish a universal price or a guaranteed delivery date because integration complexity and the state of an existing product can change the effort substantially. After reviewing the requirements, we can define the proposed workflow, dependencies, review points and handover expectations. If the project needs skills or capacity outside my scope, that should be clear before work begins.'],
  ],
  faq: [
   ['Can you work on an existing MVP?', 'Yes. The first step is to inspect the current architecture, understand the main workflow and identify the smallest useful improvement.'],
   ['Do you guarantee a launch date or commercial results?', 'No. Scope and a proposed timeline are agreed after reviewing the requirements. Customer adoption and revenue are not guaranteed.'],
   ['Who owns the code and accounts?', 'Ownership, repository access and handover are agreed in writing. Hosting and provider accounts should remain under the product owner’s control.'],
   ['Can the product use local language models?', 'Potentially. The decision depends on the hardware, latency, model quality and data requirements. PocketLLM is an example of my work in this area.'],
  ],
 },
 'nextjs-audit': {
  title: 'Next.js application audit', eyebrow: 'Understand what needs fixing, and why',
  summary: 'A repository and browser review for Next.js products: public behavior, API boundaries, accessibility, search metadata and deployment. Findings are tied to reproducible evidence and practical fixes.',
  sections: [
   ['Begin with the routes that matter', 'An application audit should start with the paths people depend on. For a portfolio, those might be opening a project, reading an article, downloading a resume and sending a message. For a SaaS product, the important path could include authentication, account setup and completing the core task. We identify those routes before deciding which checks to run. That keeps the review connected to the product rather than producing a generic list of framework preferences. The result should tell you which problems affect real use and what evidence supports each finding.'],
   ['Separate a symptom from its cause', 'A page that looks empty may have a rendering issue, a failed network request, a missing environment variable or a deliberate loading gate that never resolves. The repair depends on which of those is true. I inspect both the code and the running application to follow the behavior through its relevant boundaries. A report should include the trigger, the observed result and the expected result, with enough detail for another developer to reproduce it. Where the cause remains uncertain, the finding should say what has been verified and what still needs investigation.'],
   ['Review server and client responsibilities', 'The App Router supports server-rendered content alongside interactive client components. The useful question is which part of a page needs browser state, not whether a client directive exists anywhere in the tree. A client component can still be prerendered, but moving all content behind a mount check can leave the initial document unhelpful. I look at data fetching, loading behavior, hydration, caching and the size of the interactive surface. The goal is a page that renders useful content promptly and uses client-side state where it actually improves the experience.'],
   ['Check authorization where the data is accessed', 'Hiding an admin link or putting noindex on a page does not protect a write endpoint. The review follows requests to the handler and the data layer, checking whether the caller is authorized for the operation. It also looks for draft content that can leak through alternate lookup paths, identifiers that can escape their expected namespace and endpoints that behave differently when a configuration value is missing. Useful tests include unauthenticated requests and malformed input. Successful authenticated behavior is only one part of establishing that the boundary works.'],
   ['Inspect validation, output handling and abuse controls', 'Forms and APIs need an explicit input contract. The same field may need different treatment when stored, rendered in HTML or used in an email header. I inspect how the application handles malformed JSON, excessive payloads, unexpected fields and repeated requests. Rate limiting needs to match the hosting architecture: a process-local counter is different from a shared limit across serverless instances. External integrations should also have bounded timeouts and useful error responses. A visitor should not see a success message unless the operation that message describes has actually completed.'],
   ['Make persistence match the deployment environment', 'A local file can be a useful development content source, but it is not automatically a durable production database. The audit checks where uploads, drafts, generated results and user state live, and whether they survive the hosting platform’s lifecycle. It also checks how schema changes are applied and how an integration behaves when storage is unavailable. Where a system has a local fallback, that fallback should not silently mask a broken production write path. The recommendation is tied to the actual data and workload rather than a database preference.'],
   ['Review search metadata as a connected system', 'Titles, descriptions, canonical URLs, language alternates, social images, structured data, sitemaps and feeds should describe the same public site. I check for inherited home-page canonicals on detail pages, duplicate or invalid locale mappings, draft URLs in feeds and image routes that fail on the chosen runtime. The rendered response matters as much as the metadata object in source. Search-engine verification and sitemap submission are separate operational steps that may require account access. Implementing the files does not prove that a search engine has indexed the pages.'],
   ['Test accessibility through actual interaction', 'Accessibility review includes keyboard navigation, focus visibility, labels, heading order, contrast and the behavior of menus and dialogs. I check whether a person can reach the main content, operate important controls and recover from an error without relying on a pointer. Motion preferences matter for decorative effects and animated transitions. Responsive layout is checked with real content so that long titles and translated labels are not hidden by a tidy mockup. Automated checks can help find problems, but they do not replace opening the menu and trying to use the page.'],
   ['Measure performance in context', 'A performance finding should identify the route, environment and measurement conditions. A single score is not a complete description of the experience. I look for oversized images, missing image dimensions, unnecessary hydration gates, large client dependencies and repeated requests that delay the main content. The hero image and other likely largest-contentful elements deserve special attention. If field data is available, it can help distinguish a reproducible lab issue from a problem affecting visitors. The report should be clear about whether a result comes from a local run, a hosted test or real-user data.'],
   ['Turn findings into a practical repair order', 'An audit is easier to act on when each finding explains impact and the next concrete step. Public write access and data loss risk belong ahead of cosmetic improvements. A broken contact path may matter more to the site owner than a small improvement to an already adequate animation. I group related findings so that one coherent change can fix the underlying cause, and I avoid reporting the same problem repeatedly at different call sites. When a proposed repair has a trade-off, the report explains which behavior it changes and why.'],
   ['Verify fixes independently of the report', 'If implementation is included, each material fix should be checked against the behavior that exposed the original issue. An authorization repair needs a request that should still fail; a search repair needs a query that previously returned the wrong result; a layout repair needs the viewport where content overlapped. I run the project’s relevant checks and document failures that predate the change when they cannot reasonably be resolved in scope. After deployment, the important paths need another check because production routing and environment configuration can differ from local development.'],
   ['Agree on access, output and follow-through', 'The initial review needs repository access, an explanation of the intended behavior and a way to inspect a representative deployment. Credentials should be supplied through the appropriate account or environment controls, not pasted into an audit document. We agree whether the output is a findings report, a set of implemented fixes or both. The handover can include reproduction steps, test evidence, configuration requirements and the deployed revision. I am based in Trier and schedule work around my master’s studies, so the review window and follow-through are agreed before starting.'],
  ],
  faq: [
   ['Will you redesign the site?', 'Only when that is part of the agreed scope. A technical audit can preserve existing components and visual identity while fixing behavior.'],
   ['Can you fix the findings?', 'Yes, if implementation is included in the scope. Fixes are reviewed with tests and browser evidence appropriate to the issue.'],
   ['Will the audit guarantee a ranking or Lighthouse score?', 'No. The report identifies measurable issues and improvements. Search rankings and scores depend on factors beyond an individual code change.'],
   ['Do you need production secrets?', 'Usually repository access and a deployment are sufficient to begin. Any required credentials should be shared through scoped environment or account access.'],
  ],
 },
} as const;
