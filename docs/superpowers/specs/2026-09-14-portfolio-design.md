# Portfolio production upgrade

The user requests execution of the supplied audit and deployment. Preserve the existing component identities and hero profile card; use the supplied night meadow as the hero background, navy surfaces, violet accents and meadow green calls to action. Retain navigation, timelines, projects, skills, onboarding and footer, improving their usability.

Protect every admin page and API before loading drafts. Validate slugs and markdown, sanitize rendered HTML, deny unconfigured production writes, and support Postgres persistence with local filesystem development. Use durable rate limiting when a database is connected. Add secure image uploads through Vercel Blob.

Make portfolio information consistent with the supplied CVs. Publish both PDFs unchanged, an indexable resume, focused project case studies and useful service pages without invented outcomes or testimonials. Ground the AI assistant in that same data and published blog content. Untrusted requests cannot supply system messages or arbitrary models.

Complete canonical metadata, localized sitemap, robots exclusions, social images, structured data and RSS. Make the blog server-render useful content and provide real search, navigation, heading links, related posts and honest subscription feedback. Integrate analytics, optional monitoring, discussions, newsletter confirmation and persistent reactions where provider configuration permits.

Validate security boundaries and search with automated tests, run lint/typecheck/build, inspect desktop and mobile in a real browser, commit and deploy the verified revision. Report provider configuration and webmaster access gates separately from implemented code.
